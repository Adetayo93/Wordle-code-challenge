function submitGuess() {
    var guessInput = document.getElementById('guess-input').value;
    
    // Perform validation or further processing of the guess input
    // For now, let's just log the guess to the console
    console.log('Guess:', guessInput);
}